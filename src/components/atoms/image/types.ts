export interface Props {
  alt: string;
  src: string;
  width: number;
  height: number;
  centered?: boolean;
}

export interface PictureProps {
  imgWidth: number;
  imgHeight: number;
}

export interface StyledImageProps {
  centered: boolean;
}
